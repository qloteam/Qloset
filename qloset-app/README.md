# qloset-app (React Native + Expo)

Minimal starter for Qloset mobile app. Uses React Navigation and two example screens (Login, Home).

## Quickstart
```bash
npm i -g expo-cli
npm install
npm run start
```
