# qloset-api (NestJS + Prisma + PostgreSQL)

Minimal API with Auth (OTP stubs), Products, and Orders scaffolding. Includes Prisma schema.

## Quickstart
```bash
cp .env.example .env
npm install
npm run prisma:generate
npm run prisma:migrate:dev
npm run start:dev
```
